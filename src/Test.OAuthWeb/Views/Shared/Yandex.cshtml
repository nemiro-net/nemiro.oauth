@Ajax.ActionLink(Test.Resources.Strings.YandexDiskSummary, "Yandex", "Api", new { method = "disk" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" }) 
@Ajax.ActionLink(Test.Resources.Strings.YandexDiskContent, "Yandex", "Api", new { method = "disk-resources" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" })
@Ajax.ActionLink(Test.Resources.Strings.Photoalbums, "Yandex", "Api", new { method = "albums" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" })
