@Ajax.ActionLink(Test.Resources.Strings.FriendsList, "VK", "Api", new { method = "friends.get" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" }) 
@Ajax.ActionLink(Test.Resources.Strings.CurrentStatus, "VK", "Api", new { method = "status.get" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" }) 
@Ajax.ActionLink(Test.Resources.Strings.AudioList, "VK", "Api", new { method = "audio.get" }, new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "apiResults", OnComplete = "OnComplete()" })